interface:
  display_name: "GitHub Stacked PRs"
  short_description: "Route Git actions through multi-agent stack checks"
  default_prompt: "Use $gh-stacked-prs to perform this Git action safely, detecting repository-wide stack ownership and preserving concurrent work in existing worktrees."
